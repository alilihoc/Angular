import { FilmStarsPipe } from './film-stars.pipe';

describe('FilmStarsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilmStarsPipe();
    expect(pipe).toBeTruthy();
  });
});
